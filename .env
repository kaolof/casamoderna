# WordPress Headless Configuration
# Prefer VITE_ prefix so Vite expone la variable en build/dev
VITE_WP_DOMAIN=cms.construcasamoderna.com
# Fallback (sin prefix) por compatibilidad si lo necesitas
WP_DOMAIN=cms.construcasamoderna.com

# Otras variables de entorno pueden ir aquí
# WP_API_URL=cms.construcasamoderna.com/wp-json/wp/v2/